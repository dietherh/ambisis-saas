{
  "name": "@ambisis/types",
  "version": "1.0.0",
  "private": true,

  "main": "dist/index.js",
  "types": "dist/index.d.ts",

  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf dist"
  },

  "dependencies": {
    "zod": "^3.23.0"
  },

  "devDependencies": {
    "typescript": "^5.4.0",
    "rimraf": "^5.0.5"
  }
}